/** Minimal config present for structure parity. Tailwind is already precompiled into style.css. */
module.exports = {
  content: ["./popup.html", "./popup.js"],
  theme: { extend: {} },
  plugins: []
};
